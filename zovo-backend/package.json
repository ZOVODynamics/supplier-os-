{
  "name": "zovo-backend",
  "version": "1.0.0",
  "description": "ZOVO backend service",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "health": "node -e \"fetch('http://localhost:3000/health').then(async r => { console.log(await r.text()); process.exit(r.ok ? 0 : 1); }).catch(err => { console.error(err); process.exit(1); })\"",
    "test": "npm run health"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "uuid": "^14.0.0"
  }
}
