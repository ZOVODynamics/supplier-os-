{
  "name": "zovo-supplier-ai-backend",
  "version": "1.0.0",
  "description": "Production-ready Express backend for ZOVO Supplier AI.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check db.js && node --check routes/helpers.js && node --check routes/businesses.js && node --check routes/suppliers.js && node --check routes/requests.js && node --check routes/executions.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "helmet": "^8.2.0"
  }
}
